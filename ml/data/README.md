# Pensieve ML Data Directory

This directory is reserved for local dataset caching, exported splits, or synthetic evaluation samples if needed.

The GoEmotions dataset is automatically managed and cached via Hugging Face `datasets` under `~/.cache/huggingface/datasets/go_emotions`.
